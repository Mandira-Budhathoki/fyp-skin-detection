name: skin_app
description: A new Flutter project for melanoma detection.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
    

  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.2.0
  provider: ^6.0.6
  intl: ^0.19.0
  json_annotation: ^4.10.0

  # Image handling
  image_picker: ^0.8.7+5
  path_provider: ^2.0.14
  fluttertoast: ^8.2.1
 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  build_runner: ^2.4.6
  json_serializable: ^6.7.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
